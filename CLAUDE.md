# CLAUDE.md — fiftyvibe.kr 프로젝트 컨텍스트

> 루트 CLAUDE.md(~/.claude/CLAUDE.md)의 규칙을 상속한다. 이 파일은 Claude Code가 매 세션 시작 시 읽는 프로젝트 헌법이다. 상세 내용은 /docs 하위 문서 참조.

## 프로젝트 한 줄 요약

50세 1인 개발자(피프티바이브)가 운영하는 퇴직연금 계산 도구 사이트(계산 도구 3개 + 가이드 10편). 구글 애드센스 수익화 목표. fiftyvibe.kr 정식 배포 완료.

## 현재 상태 · 게이트 (2026-08-02)

- 로드맵 내 위치: 2층 제품화 — 도구형 웹사이트 수익화 실전 1호. 단, 애드센스 단독으로는 월 500 경로의 축이 될 수 없으므로 "실험 + 트래픽 자산"으로 취급
- **애드센스 심사 결과 대기 중** (07-31 접수, ads.txt 승인·광고 코드 검증 완료). 게이트: ~2026-08-23 — 승인 시 수익 데이터 수집 시작, 거절 시 사유 분석 후 재신청/보류 결정
- **⚠ 심사 대기 중 사이트 구조·콘텐츠 대규모 변경 금지** (심사에 영향). 신규 가이드 추가 시 상호 링크 보강 원칙 유지
- GA4/서치콘솔/네이버 서치어드바이저 등록 완료, 첫 구글 검색 노출 발생(검색어 '퇴직소득세율', 07-31~)
- 연관 이슈: coredxi.com이 애드센스 '가치가 별로 없는 콘텐츠' 플래그(08-01) — fiftyvibe 심사 완료 후 제거 여부 결정

## 문서 맵

- docs/PRD.md — 제품 요구사항 (무엇을, 왜)
- docs/SPEC.md — 기술 명세 (계산 공식, 화면, SEO)
- docs/PLAN.md — 4주 일정과 마일스톤
- docs/CHECKLIST.md — 단계별 실행 체크리스트
- docs/DECISIONS.md — 의사결정 기록. **여기 기록된 결정을 되돌리는 제안 금지. 변경이 필요하면 먼저 이유를 설명하고 운영자 승인 후 진행**

## 절대 규칙 (위반 금지)

1. **서버·DB·외부 API 추가 금지.** 모든 계산은 클라이언트에서 완결한다 (DECISIONS.md D-03)
2. **특정 금융상품(ETF 등) 추천 기능 추가 금지.** 투자자문업 규제 이슈 (D-02)
3. 세율·공제표 등 계산 상수는 반드시 `lib/tax-tables.ts` 한 파일에서만 관리. 컴포넌트에 하드코딩 금지
4. 모든 도구 페이지에 면책 고지 컴포넌트(`<Disclaimer />`) 포함
5. localStorage/sessionStorage 사용 금지 (정적 호스팅 + 개인정보 최소화 원칙). 상태는 React state로만
6. 새 npm 패키지 추가 전 정당화 필요. 목표는 최소 의존성

## 기술 스택 (확정)

- Next.js App Router + `output: 'export'` (완전 정적)
- TypeScript, Tailwind CSS
- 배포: Vercel / 분석: GA4
- 차트가 필요하면 recharts 1개만 허용

## 브랜드 토큰 (사이트 디자인에 적용)

- 네이비 `#0E1A2F` (배경/헤더), 딥네이비 `#080F1E`
- 앰버 `#F5A623` (포인트/CTA), 스틸블루 `#8FB8E8` (보조)
- 오프화이트 `#F2F6FC` (본문 텍스트, 밝은 배경에서는 네이비 텍스트)
- 시그니처: 제목 뒤 앰버 블록 커서(▮) 모티프
- 본문은 밝은 배경 + 네이비 텍스트 기본. 다크는 헤더/푸터/히어로에만

## 코드 컨벤션

- 함수형 컴포넌트, 파일명 kebab-case, 컴포넌트명 PascalCase
- 계산 로직은 `lib/calculators/*.ts`에 순수 함수로 분리하고 단위 테스트 작성 (vitest)
- 금액은 내부적으로 원 단위 정수(number)로 계산, 표시 시에만 콤마 포맷
- 커밋 메시지: `feat:`, `fix:`, `content:`, `seo:` 접두어

## 검증 게이트 (필수)

- 퇴직소득세 계산기는 SPEC.md의 검증 케이스 3종을 홈택스 모의계산과 대조해 오차 0원 확인 전까지 다음 도구 개발 금지
- 배포 전 Lighthouse 모바일 90+ 확인

## 운영자 정보 (콘텐츠 작성 시 참고)

- 페르소나: 50세 IT 대기업 부장, 내년 3월 DB→DC 전환 예정 (실사용자 관점의 E-E-A-T 소재)
- 채널: 유튜브 @fiftyvibe, 네이버 블로그 — 사이트와 상호 링크
- 법인(CoreDXI)이 소유하되 브랜드는 전면 비노출
- 콘텐츠 소재: "50대 1인개발자의 도구 사이트 애드센스 도전기" — 승인/거절 어느 쪽이든 콘텐츠화 (블로그 애드센스 시리즈가 발행 파이프라인에 등록됨)

## 세션 로그 (기록 에이전트)

- 세션 종료 시 노션 작업 로그 DB에 기록, 프로젝트 태그 `fiftyvibe`

## 백로그 · 다음 할 일

- [ ] 애드센스 심사 결과 확인 (~08-23)
- [ ] 승인 시: 광고 배치 최적화 + 월간 수익/트래픽 리포트 루틴 수립
- [ ] coredxi.com 플래그 처리 방침 결정
- 아이디어 주차장: 도구 추가(연금 수령 시뮬레이터 등), 50대 재무 도구 시리즈화
